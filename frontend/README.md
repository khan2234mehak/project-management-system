# Pulseboard — Frontend

This is the React frontend for Pulseboard. See the [root README](../README.md) for full setup instructions, architecture notes, and feature coverage.

Quick start:

```bash
cp .env.example .env
npm install
npm run dev
```
